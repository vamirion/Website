name: Keep Render App Alive

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping website
        run: |
          curl -L https://codenestsg.com || true
